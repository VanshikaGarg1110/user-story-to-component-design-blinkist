import Grid from '@mui/material/Grid';
import React from 'react';

interface WishlistProps {
    wishlistData: [];
}

const Wishlist = () => {
    return (
        <Grid>
            {/* map wishlist data and display the wishlist items. */}
            {/* <WishlistItem></WishlistItem> */}
        </Grid>
    );
};

export default Wishlist;