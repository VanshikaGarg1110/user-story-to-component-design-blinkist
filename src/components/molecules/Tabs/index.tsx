import React from 'react';

interface TabsProps {
    data: [];
}

const Tabs = (props: TabsProps) => {
    return (
        <>
            {/* map the data and display the tabs */}
        </>
    );
};

export default Tabs;